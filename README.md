# hng14-stage2-devops
# How to run

1. Install Docker & Docker Compose
2. Copy `.env` file with correct ports
3. Run: `docker compose up --build`
4. Access:
   - API: http://localhost:8000/docs
   - Frontend: http://localhost:3000